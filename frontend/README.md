# Frontend

Vue 3 + TypeScript + Vite + Tailwind CSS 4 界面。日常使用请看仓库根目录 [README](../README.md)。

```bash
npm install
npm run dev      # http://localhost:5173，配合 `uv run app --dev`
npm run build    # 产物写入 dist/，供桌面端加载
```
